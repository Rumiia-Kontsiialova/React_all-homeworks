useState — хук React, позволяет создать и обновлять состояние (state).

useCallback — хук для запоминания функции (важно для оптимизации, чтобы функция не пересоздавалась при каждом рендере).

useMemo — хук для запоминания значения, возвращаемого функцией (оптимизация повторных вычислений).

UserList — импорт компонента, который отображает список пользователей.

